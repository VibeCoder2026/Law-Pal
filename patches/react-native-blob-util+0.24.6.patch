diff --git a/node_modules/react-native-blob-util/fs.js b/node_modules/react-native-blob-util/fs.js
index 7b80f74..b44b1b8 100644
--- a/node_modules/react-native-blob-util/fs.js
+++ b/node_modules/react-native-blob-util/fs.js
@@ -112,7 +112,13 @@ function unlink(path: string): Promise {
         ReactNativeBlobUtil.unlink(path, (err) => {
             if (err) {
-                reject(addCode('EUNSPECIFIED', new Error(err)));
+                const message = typeof err === 'string' ? err : String(err);
+                if (/no such file|does not exist|failed to delete|ENOENT/i.test(message)) {
+                    resolve();
+                    return;
+                }
+                reject(addCode('EUNSPECIFIED', new Error(err)));
             }
             else
                 resolve();
